//ALERT:
//An alert() in JavaScript is a built-in method used to display a virtual pop-up dialog box containing a specified message and a single "OK" button.
//It belongs to the browser's window object. 
// Because the window object is global, you can write it simply as alert() instead of window.alert(). 
// When an alert triggers, it creates a modal window. This means it completely blocks the user from interacting with the rest of the webpage and stops the execution of code until the user clicks the "OK" button.
//The simplest way to use an alert is by passing a text string enclosed in single or double quotation marks. 
 
//Instances of JavaScript Alerts
//Example1: Basic Text Alert


alert("Welcome to our website!");

//The above code doesn't work on vs code, but it works when run on console.